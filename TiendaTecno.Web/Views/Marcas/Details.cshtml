@model TiendaTecno.Web.Models.Marca

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Marca</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.UrlLogo)
        </dt>
        <dd>
            @if (!string.IsNullOrEmpty(Model.UrlLogo))
            {
                <img src="@Url.Content(Model.UrlLogo)" alt="Image" style="width:200px;height:300px;max-width: 100%; height: auto;" />
            }

        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.MarcaID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
